module.exports = {
    description: 'Toggle visibility of subjects you are not enrolled in', 
    location: 'spoke', 
    cooldown: 5, 
    execute: async ({ client, message, args }) => {
        // todo after database restructure
    }
}